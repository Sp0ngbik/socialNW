import React from 'react';
import loader from "../../../assets/images/Gear-0.4s-200px.svg";

const Preloader = () => {
    return (
        <img src={loader} alt={'loader not found'}/>
    );
};

export default Preloader;